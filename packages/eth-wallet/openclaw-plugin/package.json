{
  "name": "@openclaw/wallet",
  "version": "0.0.1",
  "description": "Wallet plugin: tools that call the away kernel CLI (balance, send, sign).",
  "type": "module",
  "devDependencies": {
    "openclaw": "workspace:*"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
